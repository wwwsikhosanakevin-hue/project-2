import React from 'react'; import {Bar} from 'react-chartjs-2'; import {Chart as ChartJS,CategoryScale,LinearScale,BarElement,Title,Tooltip,Legend} from 'chart.js';
ChartJS.register(CategoryScale,LinearScale,BarElement,Title,Tooltip,Legend);
export default function Grades({user}){const data={labels:['Mathematics','English','Science','History'],datasets:[{label:'Performance (%)',data:[78,82,74,88]}]};return <section className="panel active-panel"><div className="panel-header"><i className="fas fa-star"></i> Grades</div><div className="card"><h3>Manage Grades</h3><p className="muted">Firebase persistence can be attached to each row using the shared CrudTable service.</p><div className="chart-container"><Bar data={data} options={{responsive:true,scales:{y:{beginAtZero:true,max:100}}}}/></div></div></section>}
